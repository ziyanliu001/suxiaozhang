/* 🌸 阳善公开滚动墙：柔和玫瑰金主题，与全站已确立的"阳善=暖色/公开"视觉语言一致。
   单行紧凑布局：左侧固定标签 + 右侧内容区水平排布，与上方阳光账本卡片的
   节奏呼应，不再是标题独占一行、内容另起一行的两段式竖排 */
.yangshan-wall-card {
  width: 100%;
  box-sizing: border-box;
  display: flex;
  align-items: center;
  background: linear-gradient(135deg, #FFF7FB 0%, #FFFFFF 100%);
  border: 1.5rpx solid #FCE7F3;
  border-radius: 20rpx;
  padding: 18rpx 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(190, 24, 93, 0.06);
}

/* 🌟 无卡片模式：去掉自带背景/边框/投影/内边距，供宿主页面把本组件嵌进
   自己的卡片容器时使用（见 bare 属性），避免"卡中卡"双层视觉 */
.yangshan-wall-card-bare {
  background: none;
  border: none;
  border-radius: 0;
  box-shadow: none;
  padding: 4rpx 0;
}

.yangshan-wall-title {
  flex-shrink: 0;
  margin-right: 16rpx;
  font-size: 24rpx;
  font-weight: 600;
  color: #BE185D;
  white-space: nowrap;
}

/* 右侧内容区：跑马灯/兜底文案的共同容器，flex:1 吃满剩余宽度，min-width:0
   允许内部文本正常触发 ellipsis 截断（否则 flex 子项默认 min-width:auto
   会被内容撑开，截断规则形同虚设——与首页公告栏踩过的同一个坑） */
.yangshan-wall-content {
  flex: 1;
  min-width: 0;
}

.yangshan-wall-swiper {
  width: 100%;
  height: 56rpx;
}

.yangshan-wall-item {
  display: flex;
  align-items: center;
  gap: 10rpx;
  height: 56rpx;
}

.yangshan-wall-name {
  font-size: 27rpx;
  font-weight: 600;
  color: #BE185D;
  flex-shrink: 0;
  max-width: 200rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.yangshan-wall-deed {
  font-size: 25rpx;
  color: #374151;
  /* flex:1 + min-width:0：物资类善行文案（如"捐赠东北大米50斤"）可能比善款
     文案更长，用弹性收缩 + 省略号兜底，避免把右侧时间标签挤出卡片 */
  flex: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.yangshan-wall-time {
  font-size: 21rpx;
  color: #9CA3AF;
  flex-shrink: 0;
}

/* 🌟 空态兜底：与 .yangshan-wall-swiper 同一份高度（56rpx），保证有/无数据
   两种状态下卡片高度一致，不会因为切换状态导致页面内容跳动；左对齐、垂直
   居中，与滚动名单的排布方式保持一致，不再单独居中显得脱节 */
.yangshan-wall-empty {
  width: 100%;
  height: 56rpx;
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: flex-start;
  overflow: hidden;
}

.yangshan-wall-empty-text {
  font-size: 25rpx;
  color: #8c6b3e;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
